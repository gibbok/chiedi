# Search benchmarks

The technical guide is maintained under [`docs/benchmarks.md`](../docs/benchmarks.md).
It covers commands, latency methodology, retrieval accuracy, and output files.

Run `make benchmark` from the repository root. Generated output is Git-ignored;
selected example reports are in [`samples/`](samples/2026-09-08-linux-xeon-8573c.md).
